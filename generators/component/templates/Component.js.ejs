'use strict';

import React from 'react';

class <%= cName %> extends React.Component {
  static propTypes = {
  };

  render() {
    return(
      <div className="<%= cName %>-Component">
        Hello from <%= cName %>
      </div>
    );
  }
}

export default <%= cName %>;
